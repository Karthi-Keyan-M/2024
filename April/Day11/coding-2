"""Write a C Program to check if two numbers are equal without using the bitwise operator."""
#include <stdio.h>

int main() {
    int x,y;
    scanf("%d %d",&x,&y);
    if(x-y==0){
        printf("Equal");
    }
    else{
        printf("Not Equal");
    }

    return 0;
}
